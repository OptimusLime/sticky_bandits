# Generated by sticky_bandits
interface={{AP_IFACE}}
bind-interfaces

# DHCP range
dhcp-range={{DHCP_START}},{{DHCP_END}},{{LEASE_TIME}}
dhcp-option=3,{{AP_ADDR}}
dhcp-option=6,{{AP_ADDR}}

# Useful logs (view via: journalctl -u dnsmasq)
log-queries
log-dhcp
